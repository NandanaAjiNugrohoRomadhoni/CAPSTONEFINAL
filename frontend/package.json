{
  "name": "capstone-frontend-sdk",
  "private": true,
  "version": "0.1.0",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{ recursive: true, force: true })\" && tsc -p tsconfig.types.json && esbuild src/index.ts --bundle --platform=neutral --format=esm --target=es2022 --outfile=dist/index.mjs && esbuild src/index.ts --bundle --platform=neutral --format=cjs --target=es2022 --outfile=dist/index.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.27.7",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
